pre-commit:
  commands:
    biome-check:
      glob: "*.{ts,tsx,js,jsx,json}"
      run: bun biome check --write {staged_files}
      stage_fixed: true

pre-push:
  commands:
    check-types:
      run: bun check-types
